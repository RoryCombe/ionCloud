// Ionic ionicMail App

// angular.module is a global place for creating, registering and retrieving Angular modules
// "ionicMail" is the name of this angular module example (also set in a <body> attribute in index.html)
// the 2nd parameter is an array of "requires"
// "ionicMail.controllers" is found in controllers.js
(function(){
    "use strict";

    angular
        .module("ionCloud", [
        	"ionic",
        	"ionCloud.filters",
            "ionCloud.controllers"
        ])
        .config(function(){
           
		});
    
})();